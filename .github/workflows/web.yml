name: Website

# Builds the static site in /web (Tailwind + release data) and deploys it to
# GitHub Pages. PRs build + validate only (no deploy). Requires GitHub Pages to
# be enabled for the repository (Settings → Pages → Source: GitHub Actions);
# Pages for a private repo needs a plan that supports it, or make the repo public.
on:
  push:
    branches: [main]
    paths: ["web/**", ".github/workflows/web.yml"]
  pull_request:
    paths: ["web/**", ".github/workflows/web.yml"]
  workflow_dispatch:

concurrency:
  group: pages
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  build:
    name: Build · Validate
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build (release data + Tailwind CSS)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run build

      - name: Validate site
        run: npm run check

      - name: Assemble deployable site
        if: github.event_name != 'pull_request' && vars.ENABLE_PAGES == 'true'
        run: |
          rm -rf _site
          mkdir _site
          cp -r *.html CNAME robots.txt sitemap.xml site.webmanifest assets data _site/
          echo "Assembled $(find _site -type f | wc -l) files for Pages"

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request' && vars.ENABLE_PAGES == 'true'
        uses: actions/upload-pages-artifact@v5
        with:
          path: web/_site

  deploy:
    name: Deploy to GitHub Pages
    # Deploys only when the maintainer has enabled Pages and set the repo
    # variable ENABLE_PAGES=true (Settings → Secrets and variables → Actions →
    # Variables). Pages needs the repo to be public or a plan that supports
    # private Pages. Until then this job is skipped so main stays green.
    if: github.event_name != 'pull_request' && vars.ENABLE_PAGES == 'true'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - name: Deploy
        id: deploy
        uses: actions/deploy-pages@v5
