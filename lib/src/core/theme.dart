import 'package:core_theme/core_theme.dart';
import 'package:flutter/widgets.dart';

/// OpenLock's identity accent — an amber "guard" palette. Built on core_theme's
/// shared shell so OpenLock looks like a sibling of the rest of the suite.
const AppAccent openlockAccent = AppAccent(
  light: Color(0xFFD97706),
  dark: Color(0xFFFBBF24),
  containerLight: Color(0xFFFEF3C7),
  containerDark: Color(0xFF451A03),
  onAccentLight: Color(0xFFFFFFFF),
  onAccentDark: Color(0xFF451A03),
);
